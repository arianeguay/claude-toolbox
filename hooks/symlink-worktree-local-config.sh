#!/usr/bin/env bash
# STUB — not implemented. Exits 0 (no-op) so the plugin installs cleanly, but it
# does NOT symlink local config into worktrees yet.
#
# SessionStart + PostToolUse(Bash) hook.
exit 0
