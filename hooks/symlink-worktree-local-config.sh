#!/usr/bin/env bash
# TODO(ariane): replace this stub with the real symlink-worktree-local-config.sh
# from your main machine's ~/.claude/hooks/. This pass-through stub exits 0 (no-op)
# so the plugin installs cleanly, but it does NOT yet symlink local config into
# worktrees — paste the real body here, then commit + bump plugin.json version.
#
# SessionStart + PostToolUse(Bash) hook: no-op until the real script is in place.
exit 0
