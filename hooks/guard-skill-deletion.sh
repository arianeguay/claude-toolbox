#!/usr/bin/env bash
# STUB — not implemented. Exits 0 (allows every Bash call) so the plugin installs
# cleanly, but it does NOT guard skill deletions yet.
#
# PreToolUse hook on Bash: reads the tool-call JSON on stdin, decides whether to
# block. Exit 0 with no output = allow.
exit 0
