#!/usr/bin/env bash
# TODO(ariane): replace this stub with the real guard-skill-deletion.sh from your
# main machine's ~/.claude/hooks/. This pass-through stub exits 0 (allows every
# Bash call) so the plugin installs cleanly, but it does NOT yet guard skill
# deletions — paste the real body here, then commit + bump plugin.json version.
#
# PreToolUse hook on Bash: reads the tool-call JSON on stdin, decides whether to
# block. Exit 0 with no output = allow.
exit 0
